// FIRESTORE_REBUILD_SPEC Step 1.4 / 6.5 — legacy phase-1 migration and
// old-schema normalization helpers removed. This file is intentionally empty;
// its former exports (normalizeTransactionPurpose, normalizeAccountFields,
// normalizeMonthlyPlanPurpose, normalizePurposeDocument) were part of the
// pre-rebuild migration path and are no longer referenced anywhere.
// (Sandbox filesystem disallows deleting the file, so it is emptied instead.)
export {};
