# Fix pass — 2026-07-25

Logic-first cross-platform fixes applied in web + Flutter.  
Statuses updated in [BUGS.md](./BUGS.md).

## Fixed

| ID | Change |
|----|--------|
| **BUG-TR-01 / TR-02** | Web transfers write `tags: ['transfer', 'transfer_to:…']` + Settlements category. Mobile pull detects Settlements / Transfer merchant / tags. Mobile push uses category **Settlements** for transfer splits (web analytics). |
| **BUG-TR-03** | Detection unified (merchant prefixes TR / Transfer to). |
| **BUG-SP-01** | Mobile pull tracks `splitCount`; sets `hasMultiSplits`. Push **skips** single-split replace when multi-split exists on server or flag set — parent amount still updates. |
| **BUG-FR-01** | `FriendMap` pushes/pulls upi, upi_ids, email, notes. Local Friend model + Hive persist. Pull uses `FriendMap.toLocal`. |
| **BUG-TX-05 / TRIP-01** | Mobile push stamps `outing-rollup` tag when note is outing total. Pull marks rollups. Detection shared via `outing_totals` + time `Trip total`. |
| **BUG-TX-04** | Status no longer hard-coded completed — preserves pending/failed/refunded when present. |
| **BUG-TX-07** | Stop title-casing account names on pull (preserve HDFC casing). |
| **BUG-ACC-01** | Account map + model include `openingBalanceDate` ↔ `opening_balance_date`. |
| **BUG-OUT-01** | Web `outingPayload` auto_add default **true** (matches UI + Flutter). |
| **BUG-OUT-02** | Outing upsert respects soft-delete (`is_active: false`); no longer always true. |
| **BUG-OUT-04** | Cancelled status preserved (not forced to completed). |
| **BUG-OUT-06** | Budget field mapped on outing push/pull. |
| **BUG-OE-02** | Split type preserves custom/percentage/shares strings when present. |
| **SYNC-01** | Description read/written on transaction map. |
| **SYNC-02** | outing-rollup + transfer tags on push. |
| **SYNC-04** | Soft-delete respected on outing upsert. |
| **SYNC-06** | Source/entrySource prefer local if set. |
| **BUG-DT-01** | `month_key` uses **UTC** date parts (align web). |
| **isTransferTransaction (web)** | Detect tags, Settlements, Transfer, merchant patterns. |

## Verify / remaining (not fully closed)

| ID | Why still open |
|----|----------------|
| BUG-SP-02 | No multi-split **UI** on mobile (data protected only). |
| BUG-ACT-01 | List UX still groups differently; rollup detection improved. |
| BUG-HOME-01/02 | Period scope / unlinked paid-by rules still differ by product. |
| BUG-HOME-03 | Verify period-totals in production. |
| BUG-TX-02 | Description on map — UI field on mobile add form still optional follow-up. |
| BUG-TX-01 / UI labels | Label copy not renamed (product choice). |
| BUG-RC-01 | Receipts still web-only. |
| BUG-CAT-01 | is_investment category sync not done. |
| BUG-OUT-05 | Settlement FIFO vs exp: note still architectural. |
| SYNC-09 / TX-08 | Rollup amount heal exists both sides — verify live. |
| BUG-ADM-01 | Needs SQL migration applied on Supabase. |
| BUG-FR-02 | Id strategy still name-v5 vs random — merge by name on pull. |

## Files touched

**Web:** `WealthPage.tsx`, `investments.ts`, `supabase-data.ts`, `outings.ts`  
**Flutter:** `supabase_schema_map.dart`, `sync_service.dart`, `outing_totals.dart`, `outing_total_refresh.dart`, `transaction_provider.dart`, `transactions_screen.dart`, `account_model.dart`, `friends_provider.dart`
